# ISM2411 Data Cleaning Copilot Project

This project demonstrates cleaning messy sales data using Python and pandas. The pipeline:
standardizes column names,
strips whitespace,
coerces numeric columns,
handles missing values,
removes invalid rows (negative values),
writes cleaned CSV to `data/processed/sales_data_clean.csv`.

## How to run
From project root:

